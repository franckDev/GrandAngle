{% extends 'base.html.twig' %}

{% block body %}
    <h1>Auteur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Nationalite</th>
                <td>{{ auteur.nationalite }}</td>
            </tr>
            <tr>
                <th>Datenaissance</th>
                <td>{% if auteur.datenaissance %}{{ auteur.datenaissance|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('auteur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('auteur_edit', { 'id': auteur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
