{% extends 'base.html.twig' %}

{% block body %}
    <h1>Auteur list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nationalite</th>
                <th>Datenaissance</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for auteur in auteurs %}
            <tr>
                <td><a href="{{ path('auteur_show', { 'id': auteur.id }) }}">{{ auteur.id }}</a></td>
                <td>{{ auteur.nationalite }}</td>
                <td>{% if auteur.datenaissance %}{{ auteur.datenaissance|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('auteur_show', { 'id': auteur.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('auteur_edit', { 'id': auteur.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('auteur_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
